```mermaid
digraph TechRoute {
  rankdir=TB;
  splines=ortho;
  nodesep=0.35;
  ranksep=0.55;
  bgcolor="white";

  node [shape=box, style="rounded", fontsize=11];
  edge [fontsize=10];

  // ===== 1) Inputs =====
  subgraph cluster_in {
    label="多源数据与输入";
    color="#BBBBBB";
    IN [label="图像 / 文本 / 属性 / 价格序列 / 用户行为"];
  }

  // ===== 2) Data Layer =====
  subgraph cluster_data {
    label="数据底座（统一建模与存储）";
    color="#BBBBBB";
    ETL [label="数据清洗与对齐（实体消歧/三元组/特征归一）"];
    NEO [label="Neo4j 图数据库（知识图谱持久化与检索）"];
    MIL [label="Milvus 向量库（语义检索）"];
  }

  // ===== 3) Algorithm Hub =====
  subgraph cluster_ai {
    label="算法中枢（模型与推理）";
    color="#BBBBBB";
    PRISM [label="PRISM 多模态图推荐\n（自适应传播/可靠性校准/全局上下文）"];
    PMT [label="PMT-Former 时序预测\n（节律编码/双注意力/频域对齐）"];
    VISION [label="视觉识别（三模型）\nEfficientNet-B3 / ConvNeXt-44 / ConvNeXt-557"];
    RAG [label="RAG 多智能体问答\n（图谱检索+向量检索+生成决策）"];
  }

  // ===== 4) Service Layer =====
  subgraph cluster_srv {
    label="中台与微服务编排";
    color="#BBBBBB";
    MID [label="Spring Boot 业务中台\nREST/WebSocket（调度/鉴权/数据分发）"];
    INFER [label="AI 推理服务\nFastAPI / ONNX Runtime / GPU 异构部署"];
  }

  // ===== 5) Apps =====
  subgraph cluster_app {
    label="应用层（多端协同）";
    color="#BBBBBB";
    WEB [label="Web 端\nWebGL 3D 图谱 / 动态编辑 / 手势交互"];
    AND [label="Android 端\n三模型可选识别 / 点击识别结果看介绍"];
    MLP [label="机器学习可视化平台\n数据集/模型管理 / 指标看板"];
  }

  // ===== Main vertical spine (make it straight) =====
  IN -> ETL -> MID;

  // ===== Data connections =====
  ETL -> NEO;
  ETL -> MIL;
  MID -> NEO [dir=both];
  MID -> MIL [dir=both];

  // ===== AI hub wired to inference service =====
  NEO -> PRISM;
  PRISM -> INFER;

  PMT -> INFER;
  VISION -> INFER;

  NEO -> RAG;
  MIL -> RAG;
  RAG -> INFER;

  MID -> INFER [dir=both];

  // ===== Apps consume via middleware =====
  MID -> WEB;
  MID -> AND;
  MID -> MLP;

  // ===== Keep services centered under data =====
  { rank=same; NEO; MIL; }
  { rank=same; PRISM; PMT; VISION; RAG; }
  { rank=same; WEB; AND; MLP; }
}

```